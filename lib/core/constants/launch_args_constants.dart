/// Windows launch argument tokens shared with native runner and installer.
class LaunchArgsConstants {
  LaunchArgsConstants._();

  /// Must match [constants/autostart_arg.txt] and [installer/constants.iss].
  static const String autostartArg = '--autostart';
}
