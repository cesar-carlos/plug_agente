#!/usr/bin/env pwsh
# Git for Windows invokes this hook; delegates to pre-push.ps1.
$hookDir = Split-Path -Parent $MyInvocation.MyCommand.Path
& (Join-Path $hookDir 'pre-push.ps1') @args
exit $LASTEXITCODE
