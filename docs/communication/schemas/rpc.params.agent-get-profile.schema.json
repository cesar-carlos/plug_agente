{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://plugagente.dev/schemas/rpc.params.agent-get-profile.v1.json",
  "title": "agent.getProfile params",
  "type": "object",
  "properties": {
    "client_token": {
      "type": "string",
      "minLength": 1,
      "description": "Optional opaque token (hex) or JWT for extra agent-side authorization. Authenticated /agents server profile sync may omit it. When supplied and enableClientTokenAuthorization is active, the agent validates it. Aliases: clientToken, auth"
    },
    "clientToken": {
      "type": "string",
      "minLength": 1,
      "description": "Alias for client_token"
    },
    "auth": {
      "type": "string",
      "minLength": 1,
      "description": "Alias for client_token"
    },
    "include_diagnostics": {
      "type": "boolean",
      "default": false,
      "description": "When true, include the optional ODBC diagnostics snapshot. Defaults to false so server profile sync stays cheap."
    }
  },
  "additionalProperties": false
}
