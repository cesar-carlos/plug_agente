{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://plugagente.dev/schemas/rpc.params.client-token-get-policy.v1.json",
  "title": "client_token.getPolicy params",
  "type": "object",
  "properties": {
    "client_token": {
      "type": "string",
      "minLength": 1,
      "description": "Opaque token (hex) or JWT whose policy is returned. Required when enableClientTokenAuthorization is active. Aliases: clientToken, auth"
    },
    "clientToken": {
      "type": "string",
      "minLength": 1,
      "description": "Alias for client_token"
    },
    "auth": {
      "type": "string",
      "minLength": 1,
      "description": "Alias for client_token"
    }
  },
  "additionalProperties": false
}
