---
description: Dart Style Guide - Effective Dart with modern best practices
globs: ["lib/**/*.dart", "test/**/*.dart"]
alwaysApply: true
---

# Dart Style Guide

## Escopo

Este arquivo e a fonte de verdade para sintaxe, naming, imports, tooling,
async/logging e recursos modernos do Dart.

O projeto usa Dart `^3.10.4`; recursos modernos disponiveis nessa constraint,
como dot shorthands, records, patterns, switch expressions, class modifiers e
null-aware collection elements podem ser usados quando melhorarem clareza.

Nao repita aqui:

- politica de comentarios e documentacao: `general_rules.mdc`
- widgets/build/layout: `flutter_widgets.mdc`
- nullability: `null_safety.mdc`

## Tipos e Legibilidade

- Declare tipos explicitamente em APIs publicas, retornos e contratos
- Prefira tipos concretos a `dynamic`
- Use `final` para referencias que nao mudam
- Use `var` apenas quando o tipo for obvio e nao reduzir clareza
- Em variaveis locais, prefira inferencia (`final`/`var`) quando o lado direito
  revela claramente o tipo; mantenha anotacao explicita quando ela documentar
  contrato, evitar ambiguidade ou melhorar a leitura
- Separe fases da funcao com poucas linhas em branco: validacao, trabalho,
  retorno
- Prefira early return para reduzir aninhamento

```dart
String formatUserName(User user) {
  if (user.firstName.isEmpty && user.lastName.isEmpty) {
    return 'Anonymous';
  }

  return '${user.firstName} ${user.lastName}'.trim();
}
```

## Funcoes, Metodos e Classes

- Mantenha funcoes pequenas e com uma responsabilidade clara
- Use expression body para membros simples
- Use parametros nomeados para opcoes e para melhorar legibilidade
- Use chaves em blocos; omita apenas em `if` de uma linha quando realmente
  couber em uma linha
- Use construtores nomeados quando houver mais de uma forma semantica de criar
  o objeto
- Use declaracao de funcao local quando precisar nomear uma closure; nao atribua
  uma lambda a variavel local se a funcao nao for reatribuida
- Para widgets imutaveis, use `const` constructor; detalhes de widget ficam em
  `flutter_widgets.mdc`

```dart
bool get isReady => _status == Status.ready;

Future<Result<User>> execute({
  required String id,
  bool forceRefresh = false,
}) async {
  if (id.isEmpty) {
    return Failure(ValidationFailure('ID cannot be empty'));
  }

  return repository.getById(id, forceRefresh: forceRefresh);
}
```

## Naming Conventions

- `UpperCamelCase` para classes, enums, typedefs, extensions
- `lowercase_with_underscores` para arquivos e diretorios
- `lowerCamelCase` para variaveis, metodos, parametros e constantes
- Nomes de funcao devem comecar com verbo: `loadConfig`, `saveQuery`
- Booleanos devem usar auxiliares: `is`, `has`, `can`, `should`, `needs`
- Membros privados usam prefixo `_`
- Parametros de callback nao usados devem virar `_`, `__`, `___`
- Evite Hungarian notation

```dart
class QueryExecutor {}

const defaultTimeout = Duration(seconds: 30);

bool get isConnected => _connection != null;
```

## Imports e Bibliotecas

- Ordem: `dart:`, `package:`, relativos
- Ordene alfabeticamente dentro de cada bloco
- Coloque `export` depois dos `import`
- Use `show` quando um import amplo prejudicar a clareza
- Nao use `library`

```dart
import 'dart:async';
import 'dart:developer' as developer;

import 'package:flutter/material.dart';
import 'package:result_dart/result_dart.dart';

import '../models/query_result.dart';
```

## Const, Final e Literais

- Use `const` para valores conhecidos em compile time
- Use `final` quando a referencia nao muda
- Use trailing commas em estruturas multiline
- O projeto usa `formatter.page_width` 120 em `analysis_options.yaml`; nao lute contra o `dart format`
- Prefira interpolacao de strings a concatenacao

```dart
const retryLimit = 3;

final String message = 'Query ${request.id} finished in ${metrics.elapsedMs}ms';
```

## Collections

- Use literais tipados: `List<T>`, `Map<K, V>`, `Set<T>`
- Evite colecoes sem tipo
- Prefira APIs de colecao declarativas (`map`, `where`, `expand`) quando
  melhorarem a leitura
- Use collection `if`, `for`, spread (`...`) e null-aware spread (`...?`) quando
  isso evitar mutacao manual sem esconder o fluxo
- Use null-aware collection elements (`?value`, `?key: value`,
  `key: ?value`) para inserir valores opcionais apenas quando presentes
- Use `isEmpty` e `isNotEmpty` em vez de comparar `.length`
- Use `whereType<T>()` para filtrar por tipo em vez de `cast<T>()` quando a
  colecao puder conter valores de outros tipos
- Evite `forEach` com function literal; prefira `for` quando houver bloco com
  varios passos ou tear-off quando for apenas repassar a chamada
- Use `StringBuffer` quando montar strings em acumulacoes maiores, loops ou
  concatenacoes frequentes

```dart
final List<String> activeIds = users
    .where((User user) => user.isActive)
    .map((User user) => user.id)
    .toList();
```

## Tooling

- Use o conjunto de lints definido no projeto; nao introduza outro baseline em
  paralelo sem decisao explicita
- Em novos projetos Flutter sem escolha definida, `flutter_lints` e um bom ponto
  de partida; em projetos Dart puro, use pelo menos `recommended`
- Rode `dart format` apos mudancas relevantes
- Use `dart fix --apply` para correcoes seguras de lint/refactor
- Rode `flutter analyze` antes de encerrar tarefas que alterem codigo Dart
- Corrija erros e avisos relevantes identificados pelo analyzer antes de fechar
  a tarefa
- Mantenha imports e codigo sem lixo morto

## Modern Dart

### Version-Aware Usage

- Use recursos modernos apenas quando a constraint de SDK do projeto permitir
- Se uma feature nova exigir language version especifica, confirme
  `environment.sdk` antes de transformar isso em padrao do codigo
- Prefira recursos modernos quando eles removem boilerplate, casts ou estados
  invalidos; evite usar sintaxe nova apenas por novidade

### Pattern Matching

- Use patterns quando simplificarem leitura e eliminarem casts
- Use `if case` para validar e destructurar ao mesmo tempo quando isso substituir
  casts ou null checks ruidosos
- Use destructuring em records e objetos quando os nomes extraidos ficarem mais
  claros que acessos repetidos
- Evite patterns muito densos; se a leitura exigir decodificar a sintaxe, extraia
  uma funcao ou valor intermediario

```dart
switch (result) {
  case Success(value: final rows):
    return rows.length;
  case Failure():
    return 0;
}
```

### Dot Shorthand

- Prefira dot shorthand quando o tipo estiver obvio pelo contexto e isso reduzir
  ruido visual
- Bons usos: enum values em atribuicoes, argumentos tipados e `switch`; named
  constructors/factories; static members; `.new()` em inicializadores cujo tipo
  ja esta explicito
- Use com mais cuidado quando a origem do simbolo puder ficar ambigua
- Evite dot shorthand com `var`, chains longas ou expressoes sem contexto de
  tipo evidente
- Evite dot shorthand se ele dificultar a navegacao ou a leitura fora do contexto

```dart
return ThemeData(
  colorScheme: .fromSeed(seedColor: Colors.blue),
);

alignment: .center;
```

### Tear-offs

- Use tear-offs quando eles deixarem callbacks e construtores mais diretos,
  legiveis e idiomaticos
- Prefira tear-off a lambda trivial que apenas repassa a mesma assinatura para
  funcao, metodo, named constructor ou unnamed constructor
- Evite tear-off quando ele esconder parametros relevantes, dificultar leitura
  ou tornar o fluxo menos obvio
- Quando uma closure precisar de nome e corpo proprio, prefira declaracao de
  funcao local em vez de variavel recebendo lambda

```dart
items.map(UserTile.new);
onPressed: controller.refresh;
```

### Records

- Use records para retornos pequenos e coesos, quando uma classe dedicada
  seria excesso
- Prefira named fields em records publicos ou retornos que precisam ser lidos
  fora de um escopo pequeno
- Destructure records perto do ponto de uso quando isso deixar o fluxo mais
  explicito
- Nao use records para entidades de dominio, estados ricos ou dados que precisam
  de invariantes, comportamento, documentacao propria ou evolucao de API

```dart
({int rows, Duration elapsed}) summarize(QueryMetrics metrics) {
  return (
    rows: metrics.rows,
    elapsed: metrics.elapsed,
  );
}
```

### Switch Expressions

- Prefira `switch` exaustivo para enums e sealed types
- Em `switch expression`, use `_` para fallback apenas quando o dominio for
  realmente aberto; para enums e sealed types, prefira deixar o analyzer apontar
  variantes faltantes
- Use guard clauses (`when`) quando a condicao complementar pertencer ao proprio
  case e mantiver o `switch` legivel

```dart
final icon = switch (status) {
  ConnectionStatus.connected => Icons.check_circle,
  ConnectionStatus.connecting => Icons.sync,
  ConnectionStatus.disconnected => Icons.error,
};
```

### Sealed Classes

- Use `sealed class` para modelar variantes fixas, como estados de UI, eventos,
  failures tipados e respostas de dominio
- Prefira sealed classes a enums quando cada variante precisar carregar dados
- Em `switch` sobre sealed types, evite `default`; deixe o compilador sinalizar
  variantes faltantes
- Mantenha subtipos diretos de uma sealed class no mesmo arquivo ou biblioteca
  quando isso ajudar a preservar a visao completa do conjunto fechado

```dart
sealed class QueryState {}

class QueryLoading extends QueryState {}

class QueryLoaded extends QueryState {
  QueryLoaded(this.rows);

  final List<String> rows;
}

class QueryError extends QueryState {
  QueryError(this.failure);

  final AppFailure failure;
}
```

### Class Modifiers

- Use `sealed` para hierarquias fechadas que devem ser exaustivas em `switch`
- Use `interface` quando a classe define um contrato para implementacoes externas
  sem expor heranca de implementacao
- Use `base` ou `final` quando a classe precisa controlar como pode ser
  estendida fora da biblioteca
- Evite modifiers por reflexo; escolha o modifier pela promessa de API que o
  tipo precisa fazer

```dart
sealed class ConnectionState {}

final class Connected extends ConnectionState {}

final class Disconnected extends ConnectionState {}
```

### Extension Methods

- Use extensions quando o comportamento fizer sentido semantico no tipo receptor
- Prefira extensoes em `BuildContext` para helpers de tema, layout e acesso a
  tokens
- Evite colocar regra de negocio ou side effects em extensoes de tipos
  primitivos
- Se o receiver nao melhorar a leitura no ponto de uso, prefira funcao nomeada

```dart
extension AppThemeContext on BuildContext {
  ColorScheme get colorScheme => Theme.of(this).colorScheme;
}
```

### Extension Types

- Considere extension types para wrappers leves com semantica forte sobre um
  valor existente, quando uma classe completa seria excesso
- Use com parcimonia em fronteiras de dominio ou IDs tipados; confirme que a
  semantica estatica melhora a seguranca sem confundir runtime e serializacao
- Prefira value objects normais quando precisar de invariantes complexas,
  comportamento rico ou compatibilidade clara com frameworks/codegen

## JSON e Codegen

- Prefira `json_serializable`/`json_annotation` quando o projeto usar models
  serializaveis
- Use `FieldRename.snake` quando a API usar `snake_case`
- Reexecute `build_runner` depois de mudar modelos anotados ou qualquer codigo
  dirigido por geracao
- Se o arquivo gerado ainda nao existir, erros como `Target of URI hasn't been
generated` sao esperados ate a primeira geracao
- Para geracao pontual, rode `dart run build_runner build
--delete-conflicting-outputs`
- Para desenvolvimento iterativo, prefira `dart run build_runner watch
--delete-conflicting-outputs`
- Nao edite manualmente codigo gerado por `build_runner`; a fonte de verdade e o
  arquivo de entrada anotado/configurado
- Se a geracao ficar inconsistente, stale ou em conflito, faca um ciclo de
  limpeza e gere novamente antes de depurar o codigo manualmente
- Sempre que a tarefa depender de geracao, confirme que o codigo gerado esta
  sincronizado antes de encerrar

```shell
dart run build_runner build --delete-conflicting-outputs
```

```shell
dart run build_runner watch --delete-conflicting-outputs
```

## Async, Erros e Logging

- Capture a excecao mais especifica que voce consegue tratar
- Preserve `error` e `stackTrace` ao traduzir falhas
- Prefira fronteiras explicitas de erro nas camadas de arquitetura
- Prefira converter excecoes para `Result` e failures nas bordas do sistema em
  vez de espalhar `try/catch` por chamadas internas e UI
- Nao use `print`; prefira `dart:developer` `log` ou o logger padrao do
  projeto

```dart
import 'dart:developer' as developer;

Future<User> loadUser(String id) async {
  try {
    final Map<String, Object?> json = await api.fetchUser(id);
    return User.fromJson(json);
  } on SocketException catch (error, stackTrace) {
    developer.log(
      'Network error while loading user',
      name: 'app.user_service',
      error: error,
      stackTrace: stackTrace,
    );
    rethrow;
  }
}
```

## Streams

- Use `Stream` para sequencias de eventos assincronos
- Feche `StreamController` e recursos associados
- Evite logica pesada dentro de listeners anonimos longos

## Referencias Cruzadas

- Comentarios/documentacao: `general_rules.mdc`
- Null safety: `null_safety.mdc`
- Widgets, `const` em UI e performance de rebuild: `flutter_widgets.mdc`
- Erros tipados e `Result<T>` do repositorio: `project_specifics.mdc`

## Checklist

- [ ] Tipos e contratos estao explicitos onde importa
- [ ] Funcoes estao pequenas e com baixo aninhamento
- [ ] Imports estao ordenados e limpos
- [ ] `const`/`final` foram usados corretamente
- [ ] Inference local (`final`/`var`) foi usada quando melhora leitura sem
      esconder contratos
- [ ] Logs usam ferramenta apropriada e preservam contexto
- [ ] O conjunto de lints definido no projeto foi respeitado
- [ ] `flutter analyze` foi executado quando a tarefa alterou codigo Dart
- [ ] Quando a tarefa depende de codegen, `build_runner` foi executado ou
      validado
- [ ] Dot shorthand foi usado apenas quando o contexto deixou o tipo obvio
- [ ] Tear-offs foram usados apenas onde melhoram a leitura
- [ ] Collections modernas (`if`, `for`, spread e null-aware elements) foram
      usadas apenas quando simplificam o fluxo
- [ ] Recursos modernos de Dart foram usados apenas quando melhoram a clareza
- [ ] Sealed classes foram usadas quando variantes fixas modelam melhor o dominio
- [ ] Class modifiers expressam uma promessa real de API
- [ ] Extensions foram criadas apenas quando o receptor era semanticamente correto
